{
  "name": "sat18-engine-v2-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for SAT18 Engine v2 monorepo",
  "scripts": {
    "install:all": "npm install && (cd server && npm install) && (cd frontend && npm install)",
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:frontend\"",
    "dev:server": "cd server && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "github:prepare": "node scripts/prepare-repo.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}